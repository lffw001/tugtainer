import { WithHostsList } from './with-hosts-list';

describe('WithHostsList', () => {
  it('should create an instance', () => {
    const directive = new WithHostsList();
    expect(directive).toBeTruthy();
  });
});
